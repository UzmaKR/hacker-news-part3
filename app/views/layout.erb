<!DOCTYPE html>
<html lang="en">
<head>
  <!-- 
    normalize.css removes cross-browser differences in defaults, e.g.,
    differences in how form elements appear between Firefox and IE
    See: http://necolas.github.com/normalize.css/
  -->
  <link rel="stylesheet" href="/css/normalize.css">
  <!--
    application.css is where you put your styles
  -->
  <link rel="stylesheet" href="/css/application.css">

  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
  <script src="/js/application.js"></script>

  <title></title>
</head>
<body>
  <nav>
    <h2> <a href="/" >Hacker News</a></h2>
    <h4> <a href="/post/new" >new</a> | <a href="/user/<%= current_user.id if current_user%>/comments" >comments</a> | <a href="/user/<%= current_user.id if current_user%>/posts" >posts</a> | 
      <% if current_user %>
        <a href="/logout" >logout</a>
      <% else %>
        <a href="/login" >login</a>
      <% end %>
    </h4>
  </nav>
  <% if current_user %>
    <%= current_user.email %> logged in
  <% end %> 
  <%= yield %>
</body>
</html>
